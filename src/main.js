// Точка входа: собираем приложение.
//
// Сборка простая, потому что архитектура «ядро + транспорты» уже всё развязала:
// UI получает транспорт (Этап 1 — Local) и работает через его интерфейс.
// Для Этапа 2 здесь же можно будет выбрать NetworkTransport — UI не изменится.

import { App } from './ui.js';

const root = document.getElementById('app');
window.__app = new App(root); // ссылка для отладки из консоли
